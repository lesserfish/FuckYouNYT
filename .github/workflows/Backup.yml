name: Backup
on:
  schedule:
    - cron: '30 10 * * *'  # Runs daily
  workflow_dispatch:      # Allows manual triggering
jobs:
  backup:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # THIS IS THE ONLY PERMISSION YOU NEED
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Backup website
        run: |
          DIR="backup"
          mkdir -p $DIR
          DATE="$(date +'%y_%m_%d')"
          # Backup Mini
          FILE="mini_$(date +'%y_%m_%d').json"
          rm -rf $DIR/$FILE
          cp -r docs/mini.json $DIR/$FILE
          cp -r docs/mini.json docs/older/$FILE
          # Backup Midi
          FILE="midi_$(date +'%y_%m_%d').json"
          cp -r docs/midi.json $DIR/$FILE
          cp -r docs/midi.json docs/older/$FILE
          echo $DATE >> docs/older/mini_list.csv
          echo $DATE >> docs/older/midi_list.csv
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add "./$DIR"
          git add "docs/older/"
          git diff --quiet && git diff --staged --quiet || git commit -m "Website backup"
          git push
